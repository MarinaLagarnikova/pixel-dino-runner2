# Архитектура

## Технологический стек

| Технология | Категория       | Назначение                            |
| ---------- | --------------- | ------------------------------------- |
| JavaScript | Язык            | Основной язык программирования        |
| HTML       | Язык            | Разметка и структура документа        |
| CSS        | Язык            | Стилизация и компоновка               |
| NPM        | Пакетный менеджер | Управление зависимостями и скриптами |
| ESLint     | Линтер          | Контроль качества и стиля кода        |
| jscpd      | Анализ          | Обнаружение дублирования кода         |
| Jest       | Тестирование    | Фреймворк модульного тестирования     |

## Спрайты

Глобальный масштаб: `SPRITE_SCALE = 0.35` (все спрайты рисуются в 35% от оригинала).

### Спрайтшиты

| ID            | Файл                                |
| ------------- | ----------------------------------- |
| dino          | `assets/spritesheet.png`            |
| cactus        | `assets/cactus-spritesheet.png`     |
| pterodactyl   | `assets/pterodactylus-spritesheet.png` |

### Фреймы

#### Дино — бег (3 фрейма, 100ms/кадр)

| #  | sx  | sy  | sw  | sh  | anchorX | anchorY | sheet |
| -- | --- | --- | --- | --- | ------- | ------- | ----- |
| 0  | 235 | 181 | 322 | 192 | 161     | 192     | dino  |
| 1  | 598 | 181 | 279 | 192 | 140     | 192     | dino  |
| 2  | 960 | 181 | 314 | 192 | 157     | 192     | dino  |

#### Дино — прыжок (1 фрейм)

| sx  | sy  | sw  | sh  | anchorX | anchorY | sheet |
| --- | --- | --- | --- | ------- | ------- | ----- |
| 322 | 549 | 249 | 165 | 125     | 165     | dino  |

#### Дино — пригибание (1 фрейм)

| sx  | sy  | sw  | sh  | anchorX | anchorY | sheet |
| --- | --- | --- | --- | ------- | ------- | ----- |
| 756 | 651 | 272 | 100 | 136     | 100     | dino  |

#### Маленький кактус (1 фрейм)

| sx  | sy  | sw  | sh  | anchorX | anchorY | sheet  |
| --- | --- | --- | --- | ------- | ------- | ------ |
| 234 | 404 | 94  | 125 | 47      | 125     | cactus |

#### Большой кактус (1 фрейм)

| sx | sy | sw  | sh  | anchorX | anchorY | sheet  |
| -- | -- | --- | --- | ------- | ------- | ------ |
| 48 | 74 | 143 | 230 | 71      | 230     | cactus |

#### Птеродактиль (3 фрейма, 125ms/кадр)

| #  | sx   | sy  | sw  | sh  | anchorX | anchorY | sheet       |
| -- | ---- | --- | --- | --- | ------- | ------- | ----------- |
| 0  | 207  | 250 | 298 | 241 | 149     | 241     | pterodactyl |
| 1  | 538  | 353 | 480 | 137 | 240     | 137     | pterodactyl |
| 2  | 1057 | 379 | 355 | 133 | 177     | 133     | pterodactyl |

### Итоговые размеры на экране (при SPRITE_SCALE = 0.35)

| Спрайт            | Размер (px)  |
| ----------------- | ------------ |
| Дино (бег)        | ~113 x 67    |
| Дино (пригибание) | ~95 x 35     |
| Маленький кактус  | ~17 x 35     |
| Большой кактус    | ~25 x 50     |
| Птеродактиль      | ~46 x 40     |

### Как читать координаты

Каждый фрейм описывает прямоугольник на спрайтшите:

- **sx, sy** — верхний левый угол области вырезки
- **sw, sh** — ширина и высота вырезки
- **anchorX, anchorY** — точка привязки (пивот) относительно левого верхнего угла спрайта; используется для позиционирования на сцене (anchor всегда по нижнему центру/нижнему краю)

## Архитектурный паттерн

Функциональные модули с явной передачей состояния. Нет классов, нет `this`.
Единственное глобальное состояние создаётся в `main.js` и передаётся в функции явно.

## Компоненты

| Модуль | Ответственность | Граница |
|--------|----------------|---------|
| `constants.js` | Все настраиваемые параметры игры | Только export, нет логики |
| `gameLoop.js` | RAF + delta-time, вызов update/render | Не знает о состоянии игры |
| `renderer.js` | Отрисовка сцены на canvas | Только читает состояние, не меняет |
| `main.js` | Точка входа, инициализация, сборка | Единственный владелец глобального состояния |
| `ground.js` | Скроллинг земли | updateGround (чистая) + drawGround |
| `background.js` | Параллакс фон | updateBackground (чистая) + drawBackground |
| `dino.js` | Физика и анимация дино | updateDino(dino, input, dt) — прыжок, пригибание, гравитация + drawDino |
| `score.js` | Счёт и скорость | updateScore, updateSpeed (чистые) + localStorage |
| `input.js` | Клавиатура | createInput() — замыкание с Set |
| `sprites.js` | Спрайтшиты | loadSprites(), drawSprite(), DINO_RUN_FRAMES |
| `ui.js` | UI элементы | drawStartScreen, drawScore, drawPausedScreen, drawGameOverScreen |
| `obstacles.js` | Спавн, движение, коллизия и отрисовка всех препятствий (кактусы + птеродактили) | createObstacles, updateObstacles(obstacles, speed, dt, score?), checkCollision, drawObstacles |

## Форма состояния

```js
{
  status: 'idle' | 'running' | 'paused' | 'dead',
  dino: { x, y, vy, state: 'run' | 'jump' | 'duck', frame, frameTimer },
  ground: { x },
  background: { elements: [{ x, y, w }] },
  score: { current, high },
  speed: number,
  obstacles: {
    list: [
      { x, type: 'small' | 'large' | 'double' }                                    // кактус
      | { x, type: 'ptero_low' | 'ptero_mid' | 'ptero_high', y, frame, frameTimer } // птеродактиль
    ],
    spawnTimer,
  },
}
```

## Паттерн обновления состояния

`update(state, input, dt)` → новый объект состояния (иммутабельно).
`main.js` присваивает `state = update(state, input, dt)`.
Каждый модуль экспортирует `update*` — чистую функцию над своим срезом состояния.

## Поток данных

```
input.js → main.js (команды)
main.js → update-функции → новое состояние
новое состояние → renderer.js → canvas
```

## Правила

- Каждый модуль экспортирует только чистые функции
- Состояние передаётся явно, никогда не хранится в модулях
- Все числовые параметры — в `constants.js`
- Нет закомментированного кода
